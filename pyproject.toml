[project]
name = "baby-gpt"
version = "0.1.0"
description = "A baby GPT implementation in a Jupyter notebook"
requires-python = ">=3.13"
dependencies = [
    "jupyterlab>=4.4.9",
    "matplotlib>=3.10.7",
    "torch>=2.9.1",
]

[tool.uv]
package = false
